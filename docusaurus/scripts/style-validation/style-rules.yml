# Legacy config file kept for compatibility with scripts and docs that reference this path.
#
# Rules (same five as legacy rule-parser.js):
#   - easy/casual/simple/pronouns: lib/legacy-style-rules.js (exact rule-parser parity)
#   - inline enumerations: Vale (docusaurus/styles/Strapi/InlineEnumerations.yml, 5+ items)
#   - Vale runner: docusaurus/scripts/vale/run.sh (via validate-content-style.js)
#   - Config: docusaurus/.vale.ini
#
# Run validation:
#   node scripts/style-validation/validate-content-style.js [--verbose] [files...]

version: "2.0"
name: "Strapi 12 Rules (Vale-backed)"
description: "See docusaurus/styles/Strapi/ and .vale.ini for active rules"
