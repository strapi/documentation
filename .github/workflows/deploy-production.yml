name: Deploy Production version
on:
  push:
    branches:
      - main

jobs:
  pre-build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: './docusaurus/package-lock.json'  # Ajout du './'

      - name: 📦 Install dependencies
        working-directory: ./docusaurus
        run: npm ci

      - name: 🤖 Generate and commit LLMs files
        working-directory: ./docusaurus
        run: |
          # Generate files
          node scripts/generate-llms.js
          
          # Configure git
          git config --local user.email "action@github.com"
          git config --local user.name "LLMs Generator"
          
          # Commit if changed
          git add static/llms*.txt
          if ! git diff --staged --quiet; then
            git commit -m "🤖 Update LLMs files [skip ci]"
            git push
            echo "✅ LLMs files updated - Vercel will auto-redeploy"
          else
            echo "🔄 No changes in LLMs files"
          fi

  deploy-prod:
    runs-on: ubuntu-latest
    needs: pre-build
    steps:
      # Le reste de ton workflow...
